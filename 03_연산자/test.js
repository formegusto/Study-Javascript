/*
 = 대입연산자
 + 덧셈
 - 뺄셈
 * 곱셈
 / 나눗셈
*/
let a = 1 + 2 - (3 * 4) / 4;
console.log(a);

/*
 a++ 후위 증감
 ++a 전위 증감
 = 대입연산자
 +=
 *=
*/

/*
 ! NOT
 && AND
 || OR
 연산 순서는
 NOT -> AND -> OR
*/

/*
 === 값의 일치 확인 + 타입검사
 == 값의 일치 확인
 undefined와 null도 같은 값으로 간주한다.
 !== 두 값이 일치하지 않는지 확인 + 타입검사
 != 쓰기 귀차늠
*/

// + 문자열 붙이기도 가능